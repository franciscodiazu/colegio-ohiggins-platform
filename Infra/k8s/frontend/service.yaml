apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: colegio-ohiggins
  labels:
    app: frontend
    tier: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
