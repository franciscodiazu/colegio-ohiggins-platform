apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: colegio-ohiggins
  labels:
    app: mysql
    tier: data
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
